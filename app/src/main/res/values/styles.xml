<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ChartDialog" parent="@style/Theme.AppCompat.Light.Dialog">
        <item name="android:windowNoTitle">false</item>
        <item name="android:windowTitleStyle">@style/ChartDialogTitle</item>
    </style>

    <style name="ChartDialogTitle">
        <item name="android:titleTextStyle">@style/ChartDialogTitleTextStyle</item>
        <item name="android:textColor">@color/cardMatchTourName</item>
        <item name="android:layout_marginBottom">@dimen/chart_margin</item>
    </style>

    <style name="ChartDialogTitleTextStyle">
        <item name="android:textSize">18sp</item>
    </style>
</resources>
